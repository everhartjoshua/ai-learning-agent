# ──────────────────────────────────────────────────────────────
# AI Learning Agent — Python dependencies
# ──────────────────────────────────────────────────────────────
# Pinned for reproducible container builds. To upgrade, bump a
# version here, run the test suite, and submit a PR — never
# bump silently in a deploy.

# ── Web framework + server ────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6   # [standard] adds uvloop + httptools (faster HTTP/event loop on Linux)
pydantic==2.9.2

# ── Frontend ──────────────────────────────────────────────────
streamlit==1.39.0
httpx==0.27.2               # used by Streamlit to call the FastAPI backend

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.35

# ── LLM client + vector store ─────────────────────────────────
openai==1.51.0              # SDK speaks OpenAI-compatible APIs (works for Groq, Ollama, OpenAI)
chromadb==0.5.15

# ── Config + scripts ──────────────────────────────────────────
python-dotenv==1.0.1
rich==13.9.1                # pretty terminal output (used by scripts/)
typer==0.12.5               # CLI framework (used by scripts/)
